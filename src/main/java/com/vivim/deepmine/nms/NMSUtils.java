package com.vivim.deepmine.nms;

import org.bukkit.entity.Player;

public class NMSUtils {
    //public void sendPacket(Player p, Packet packet) {
    //    return ((CraftPlayer) p).getHandle().playerConnection().sendPacket(packet);
    //}
}
